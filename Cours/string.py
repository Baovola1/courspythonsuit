#Les fonctions  en string
#len()=> Savoir le nbr de caractère dans une chaine de caractère
texte = 'hello'
print(len(texte))
#L'indexing des strings
#index maximal: index du dernier element=>len(texte)-1
#index négatif: -len(texte)=>limite a ne pas dépaC